# Pour déployer: kubectl --kubeconfig votre-conf.yaml apply -f deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: adia
  name: frontend-demo-deployment-adia
  labels:
    demo.app: frontend-demo-adia
spec:
  replicas: 2
  selector:
    matchLabels:
      demo.app: frontend-demo-adia
  template:
    metadata:
      labels:
        demo.app: frontend-demo-adia
    spec:
      containers:
        - name: frontend
          image: adiadev/frontend:latest
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  namespace: adia
  name: frontend-demo-adia
spec:
  selector:
    demo.app: frontend-demo-adia
  ports:
    - protocol: TCP
      port: 4566
      targetPort: 80

---

apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  namespace: adia
  labels:
    app.kubernetes.io/component: controller
  # Mettre un nom unique pour la classe ingress
  name: nginx-demo-adia-frontend
  annotations:
    ingressclass.kubernetes.io/is-default-class: "true"
spec:
  controller: k8s.io/ingress-nginx

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-demo-ingress
  namespace: adia
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging-cluster-issuer
spec:
  # Récupérer le nom de la classe ingress ici
  ingressClassName: nginx-demo-adia-frontend
  tls:
    - hosts:
        - frontend.4al2.adia.jbienvenu.fr
      secretName: frontend-demo-tls
  rules:
    - host: frontend.4al2.adia.jbienvenu.fr
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: frontend-demo-adia
                port:
                  number: 4566

#51.159.205.16
